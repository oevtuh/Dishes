@using Dishes.Helpers

@*@model IEnumerable<Models.Ingredient>*@

@{
    ViewBag.Title = "title";
    Layout = "../Shared/_Layout.cshtml";
}




<html>
<head>

    <script src="http://code.jquery.com/jquery-1.8.3.js"></script>


    <link rel="stylesheet" href="../../Content/chosen.css">
</head>
    <body>
        @*'@Url.Action("Dish", "Dishes")'*@
    
        
       
        @*<p>Full name: <strong data-bind="text: fullName"></strong></p>*@

        @*<button data-bind="click: addSeat">Reserve another seat</button>
        <button data-bind="click: addSeat2">Reserve another seat2</button>*@
        

        @*<select class="chzn-select" multiple="true" name="faculty" style="width:200px;">
            @foreach (var ingredient in Model)
            {
                <option value=@ingredient.Id>
                    @ingredient.Name
                </option>
            }

        </select>*@
        @Html.Action("Choose", "Ingredients");
        @*<div class="dropdown">
            <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-expanded="true">
                Dropdown
                <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu3">
                <input type="checkbox"/>dfff
            </ul>
        </div>*@
        


        <ul id="dishesList"></ul>

        <div class="panel panel-default" data-bind="visible: dishes().length">
            <div class=" panel-heading">Блюда, которые можно приготовить</div>
            <div class="panel-body">
                <ul data-bind="foreach: dishes">
                   
                    <li><a data-bind="text: Name, attr: { href: '@Url.AbsoluteAction("Dish", "Dishes")'.replace('{id}', Id)}"></a></li>


                </ul>
            </div>
        </div>
    
    
    </body>
</html>

@section scripts{
    
    @Scripts.Render("~/bundles/knockout")
    <script src="../../Scripts/knockout/dishesViewModel.js"></script>

 
    @Scripts.Render("~/bundles/choose")
   
  
}