@using Dishes.Helpers
@model IEnumerable<Models.DishCategory>

@{
    ViewBag.Title = "Alldishes";
    Layout = "../Shared/_Layout.cshtml";
}


@*<div class="row">
        @foreach (var dish in Model)
        {
            <div class="col-sm-6 col-md-4">
            <div class="thumbnail">
                <a href="@Url.Action("Dish", "Dishes", new { id = dish.Id})"><img src="/content/images/@dish.Image" alt="..."></a>
                <div class="caption">
                    <h3>@dish.Name</h3>
                    <p>@dish.ShortDescription</p>
                    <p><a href="@Url.Action("Dish", "Dishes", new { id = dish.Id})" class="btn btn-primary" role="button">Подробнее</a> <a href="#" class="btn btn-default" role="button">Button</a></p>
                </div>
            </div>
        </div>
        }
    </div>*@
<div class="all-dishes">

    <a href="@Url.Action("DishesInCategory", "Dishes")">
        Show all dishes
    </a>

</div>


<div class="">
    @foreach (var category in Model)
    {
        <div class="" style="">
            <div class="">
                @*<a href="@Url.Action("Dish", "Dishes", new { id = dish.Id})"><img src="/content/images/@dish.Image" alt="..."></a>*@
                <div class="caption">
                    <a href="@Url.Action("DishesInCategory", "Dishes", new {categoryId = category.Id})">
                        <h3>@category.Name</h3>
                    </a>
                    <img src="/content/images/@category.Image" alt="...">

                    @*<p><a href="@Url.Action("Dish", "Dishes", new { id = dish.Id})" class="btn btn-primary" role="button">Подробнее</a> <a href="#" class="btn btn-default" role="button">Button</a></p>*@
                </div>
            </div>
        </div>
    }
</div>




